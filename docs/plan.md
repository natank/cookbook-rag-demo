this plan is scaffolding the way forward for this demo project.

## next refactors
- create a setup of minimal browser ui for the demo, working against a local API server (uv) that can handle the ingestion and retrieval requests. This will allow users to interact with the RAG system through a web interface, making it more accessible and user-friendly.